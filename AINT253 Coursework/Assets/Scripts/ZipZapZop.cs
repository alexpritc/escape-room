using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZipZapZop : MonoBehaviour
{
    // When?
    // - NPC is talking.
    // - PC is on
    // - Fuse box puzzle is complete.

    // What?
    // Spawn 2D plane in front of object on the axis that the player is on,
    // the rotation depends on the players transform position.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
